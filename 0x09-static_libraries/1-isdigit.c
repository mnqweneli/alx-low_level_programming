#include "main.h"

/**
 * _isdigit - checks for a number(digit) from 0 to 9
 * @c: number to be checked
 *
 * RETURN: 1 (on success), 0 if not
 * Author: MNQWENELI qABO
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
